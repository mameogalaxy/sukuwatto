# figures アセット

人物のアバターは現状アプリ内で絵文字として表現しているため、ここには画像を置いていません。

将来、人物ごとの肖像画像（ライセンスに配慮した素材）を使う場合は、
ここに `oda_nobunaga.png` のように `HistoricalFigure.id` と対応するファイル名で配置し、
`HistoricalFigure` に `imageAsset` フィールドを追加して参照してください。
